## Updated content
- Display the name of the pumping miner and report the computing power
- Judging difficulty. Only the latest is accepted.
- Optimized pumping algorithm. Changed from a fixed algorithm to a random algorithm.
- Judge Diff difficulty. Only the latest is accepted.
- share_alg: 0 #Pumping algorithm. 0 is random algorithm 1 is fixed share algorithm.
- Optimize the pumping algorithm and update the new pumping mode.
- Added terminal to print the computing power for ten minutes.